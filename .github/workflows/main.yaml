name: first github actions 

on:
    push:
        branches: ["main"]
jobs:
    build:
        runs-on: windows-latest
        
        steps:
        - name: checkout action
          uses: actions/checkout@v3
